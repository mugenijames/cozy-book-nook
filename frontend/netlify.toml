[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

[build]
  publish = "dist"              # ← must match Vite's output folder
  command = "npm run build"     # or "vite build"